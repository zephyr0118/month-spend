# 更新记录

## 1.4.2 — 2026-08-09

- 新增 `daily` 日用构建：沿用现有 Debug 包名与调试签名以支持覆盖安装，但关闭调试运行时，并启用 R8 代码优化和资源压缩，消除 Debug Compose 在真实手机上的明显性能损耗。
- Compose BOM 升级至 `2026.06.00`；Room 升级至 `2.8.4` 以兼容新版 Kotlin 元数据，并获得 Room 2.8 系列的查询与线程调度性能修复。
- Android 15 及以上为实际 Compose 视图树逐层投票设备最高刷新率，同时启用窗口触摸升频并关闭该窗口的自适应刷新率省电平衡；避免 View 默认 `Normal` 投票把滚动维持在约 60Hz。
- 首页、分析页、设置页、图表和记账面板缓存分类筛选、账户列表、流水分组、分析聚合及图表派生数据，减少滚动期间的对象分配和主线程计算。
- 日用包提升为 `versionCode 7`、版本 `1.4.2-daily`，包名仍为 `com.yueji.finance.debug`，可覆盖安装并保留原数据。

## 1.4.1 — 2026-08-09

- 修正窗口刷新率提示冲突：不再同时设置会互相覆盖的显示模式 ID 与刷新率，改为清空模式 ID、单独请求最高刷新率，并在窗口重新获得焦点时重申请求。
- 设置页新增“高刷新率”入口，可直达系统显示设置；一加 12/OxygenOS 用户可继续在“屏幕刷新率 → 高 → 每应用刷新率”中为月迹指定 120Hz。
- 流水和首页最近账单改为商户/收入来源优先作为主标题；未填写来源时才回退显示分类，已填写时分类移到日期、账户所在的次要信息行。
- Debug 包提升为 `versionCode 6`，包名和签名保持不变，可覆盖安装 1.4.0。

## 1.4.0 — 2026-08-09

- 月末预测新增预计收入，并将收入汇总与支出汇总口径完全拆开；月度/季度/年度收入不会再阻止日常支出的月末外推。
- 月预算新增“固定预算 / 动态预算”切换。动态模式按“基础预算 +（基础预算 − 上月实际支出）”生成本月额度；上月没有真实支出记录时不做结转。
- 首页和目标页明确展示基础预算、正负结转额及最终动态预算，预测超支风险统一对比最终预算。
- 转账和余额调整不再保存支出分类；流水使用独立的转账/调整图标，3→4 migration 会清理旧转账错误继承的分类。
- 商户与收入来源输入接入后台商户库；每次保存会规范化名称、累计使用次数、记录最近时间和常用分类，后续输入支持包含式模糊查询与一键选择。
- 窗口按当前物理分辨率主动申请设备支持的最高刷新率，并显式启用硬件加速；流水分组结果改为记忆化，减少滚动期间的重复分配。
- Room schema 提升至 v4，Debug 包提升为 `versionCode 5`，包名及签名保持不变，可覆盖安装旧版。

## 1.3.0 — 2026-08-05

- 首页预算主卡同时显示预算剩余金额与剩余百分比；超支时改为显示超预算百分比。
- 记账面板跳过半展开状态，点击“记一笔”后直接完整展开，系统返回手势/返回键一步关闭。
- 日期和商户/收入来源提升为始终可见的主要字段；点击日期按钮直接打开日历，同时保留今天、昨天快捷项。
- 新增默认付款账户设置；未设置时仍可自动沿用最近账户，设置值会进入 DataStore 和完整备份。
- 账户管理新增上移、下移排序；编辑账户会保留原顺序和创建时间，新账户自动排在末尾。
- 流水和首页最近账单会展示明确日期、商户/来源与付款账户。
- 流水查询新增分类真实 `iconKey` 投影，账单图标严格跟随所选分类，不再根据名称推测。
- 支出与收入新增按天、周、月、季、年记录粒度；收入切换时默认按月，适合工资、奖金等汇总录入。
- 所选日期决定汇总归属周期，今天、昨天和日历入口继续保留；流水明确显示“某月/某季度/某年汇总”。
- 商户/支出去向及收入来源改为始终可见，提供明确示例和说明；汇总补录会提示避免与逐日记录重复统计。
- Room 升级到 schema v3，增加 `recordGranularity`、`periodStartEpochDay`、`periodEndEpochDay` 及显式 2→3 migration；CSV 同步支持新字段。
- 增加周期推导、分类图标投影、账户排序、汇总统计与迁移回归测试；Debug 包提升为 `versionCode 4`。

## 1.2.0 — 2026-08-05

- 目标支持原位编辑：点击目标卡或铅笔按钮即可修改类型、名称、金额/储蓄率及截止日期，更新时保留目标 ID、创建时间与历史账单。
- 目标删除增加二次确认，避免日常使用时误触；目标卡增加截止日期、独立编辑入口及更准确的储蓄率完成进度。
- 记账面板改为固定底部保存区，滚动分类和补充信息时保存按钮始终可见。
- 日期输入改为“今天 / 昨天 / 日历选择”，不再要求手工输入 ISO 日期。
- 金额数字键盘加入轻触觉反馈；新账单会自动沿用最近使用的有效账户，并在交易类型一致时沿用最近分类。
- 首页增加最近三笔账单，可直接点击进入编辑，保存后可以立刻核对结果。
- 新增目标同 ID 更新回归测试；Debug 包版本提升到 `versionCode 3`，继续沿用包名和签名，可覆盖 1.0/1.1。

## 1.1.0 — 2026-08-05

- 完成一轮大规模 UI/UX 升级：采用暖黄色品牌视觉、低阴影信息卡、统一圆角与更清晰的收支层级，并同步更新启动图标。
- 重做首页：本月支出、收入、结余和预算进度集中展示，新增 8 个高频分类快捷记账入口。
- 重做记账面板：增加免唤起系统键盘的金额数字键盘、图标分类网格、常用账户区和可折叠补充信息，保留五类交易、复制上一笔、附件及完整支出属性。
- 优化流水页：新增月度收支摘要头卡、圆形搜索框、分类图标账单卡及更明确的正负金额反馈。
- 修复月预算修改后仍显示旧值的问题；更新时统一写入稳定预算记录，并自动清理旧版本产生的同月重复预算。
- Debug 安装包保持 `com.yueji.finance.debug` 和原调试证书不变，`versionCode` 升至 2，可覆盖安装 1.0.0 并保留本地数据。
- 新增月预算覆盖更新数据库回归测试；通过 13 项 JVM 测试、Android 测试源码编译与 Lint（0 error）。

## 1.0.0 — 2026-08-04

- 建立可编译、可安装的 Android 原生工程及完整离线数据层。
- 实现首次启动、历史数据、五类交易、账户分类、首页、流水、分析、目标、CSV、备份恢复、报表、提醒、应用锁、主题和自适应布局。
- 引入 Room schema v2、1→2 migration、历史验收测试、核心口径测试、数据库/UI 测试与 CI。
- 已验证 Debug/Release 编译、JVM 测试及 Android 测试源码编译。
