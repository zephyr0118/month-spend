# Windows 一键打包与局域网分发

仓库根目录现在提供两个入口：

```text
一键打包并局域网分发.bat
完整清理并重新打包.bat
```

## 平时使用：快速增量构建

日常修改完代码后，直接双击：

```text
一键打包并局域网分发.bat
```

它默认执行：

```powershell
.\gradlew.bat :app:assembleDaily
```

不会再先执行 `clean`，Gradle 会复用上一次的构建缓存和未变化的中间产物，只重新处理发生变化的部分，因此第二次及之后的构建通常会明显更快。

第一次构建、依赖发生变化、R8 重新处理较多内容时仍可能需要较长时间，这是正常现象。

## 特殊情况：完整清理后重新构建

只有遇到下面这些情况时，才建议双击：

```text
完整清理并重新打包.bat
```

它会执行：

```powershell
.\gradlew.bat :app:clean :app:assembleDaily
```

适合以下情况：

- Gradle 缓存或中间产物疑似异常；
- 明明修改了代码但安装后表现仍像旧版本；
- 出现难以解释的资源、R8、编译缓存问题；
- 需要确认一次完全从干净状态生成的 APK。

完整清理构建会明显更慢，不建议每次更新代码都使用。

## 两种模式都会自动完成什么

无论使用快速模式还是完整清理模式，后续流程完全一样：

1. 构建项目现有的 `daily` APK；
2. 将最新 APK 复制为固定文件：

```text
dist\yueji-latest.apk
```

3. 如果电脑已经通过 ADB 授权连接且只检测到一台 Android 设备，则自动执行：

```powershell
adb install -r dist\yueji-latest.apk
```

这会覆盖安装当前 Daily 版本并保留应用数据。

4. 自动识别电脑当前局域网 IPv4 地址，并启动本地局域网 HTTP 下载服务；
5. 控制台会显示类似：

```text
http://192.168.1.23:8765/
```

手机与电脑处在同一个 Wi-Fi / 局域网时，在手机浏览器打开该地址即可进入下载页并下载最新 APK。

同时还会显示 APK 直链，例如：

```text
http://192.168.1.23:8765/yueji-latest.apk
```

## 覆盖安装说明

当前脚本构建的是项目现有的 `daily` 变体。该变体继续使用：

```text
com.yueji.finance.debug
```

并沿用同一调试签名，因此可以覆盖目前的日用版安装并保留 Room 数据库和 DataStore 设置。

不要先卸载旧版。Android 卸载应用时会删除应用私有数据。

## Windows 防火墙

第一次启动局域网下载服务时，Windows Defender 防火墙可能弹出联网提示。

请选择允许“专用网络”访问。无需开放公网，也无需进行路由器端口映射。

如果手机无法打开下载地址，请依次确认：

- 手机和电脑连接的是同一个 Wi-Fi / 局域网；
- Windows 当前网络类型最好设置为“专用网络”；
- 防火墙没有阻止 PowerShell；
- Wi-Fi 路由器没有启用“AP 隔离 / 客户端隔离”；
- VPN、Clash、虚拟网卡没有改变局域网路由。

## 端口

脚本默认使用：

```text
8765
```

如果端口被占用，会自动依次尝试 `8766` 到 `8785`。

## 停止下载服务

保持脚本窗口打开时，局域网下载地址持续有效。

需要停止时，在窗口中按：

```text
Ctrl + C
```

即可。

## 打包失败时

两个入口在真正执行主脚本前都会先做一次 PowerShell 语法检查。

脚本仍然使用项目原有 Gradle/Android 构建环境，因此电脑需要满足项目正常构建的基础条件，包括 Java 17、Android SDK 36.1 以及项目所需的 Build Tools。

如果普通增量构建出现疑似缓存问题，可以先尝试一次 `完整清理并重新打包.bat`。如果完整清理后仍然失败，再根据窗口中的 Gradle 错误继续排查。
