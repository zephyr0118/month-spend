# 「月迹」安卓个人财务监控 App
## 产品需求、交互设计与 Codex 实现规格

> 文档用途：直接交给 Codex 生成一个可运行、可持续迭代的 Android 原生应用。  
> 产品代号：月迹（可后续更名）  
> 默认语言：简体中文  
> 默认币种：人民币 CNY  
> 核心定位：以“按月掌握花费、结余和资产增长”为中心的本地优先个人财务 App。  
> 文档版本：V1.0  
> 目标平台：Android 手机，兼容平板与折叠屏布局。

---

## 1. 产品背景

用户目前已经按年度整理了总余额、收入、消费、结余及各账户余额，但统计周期主要为“每年 8 月至下一年 8 月”的年度跨度。现需要将记账和分析粒度下沉到“月”，并同时保留年度视角。

App 必须解决以下问题：

1. 随时知道本月已经花了多少钱。
2. 知道本月还能花多少钱，以及按当前速度月底可能花多少。
3. 同时设置月消费、年消费、月结余、年结余、月收入、年收入、资产余额等目标。
4. 对消费增长、收入增长、储蓄率变化、账户余额分布等进行多维分析。
5. 直观展示“钱花到了哪里”“比上月多花在哪里”“目标是否可能完成”。
6. 保留用户 2023—2026 年现有历史数据，同时避免虚构不存在的月度流水。
7. 默认全部数据保存在本机，使用简单、界面精美、录入成本低。
8. 后续能够扩展 CSV 导入、自动识别交易、票据识别、云同步等能力。

---

## 2. 产品原则

### 2.1 月度优先，年度联动

首页默认展示当前月，不以年度总表作为首页。年度数据作为趋势、目标和复盘视角存在。

### 2.2 不伪造历史数据

用户现有历史数据只有年度汇总和每年 8 月账户余额快照。不得把年度消费简单除以 12 后生成 12 条“真实月消费”。

历史数据应分为：

- **余额快照**：真实输入的某月各账户余额。
- **年度汇总**：真实输入的收入、消费、结余。
- **月度流水**：从启用 App 后逐笔录入或导入的真实数据。
- **估算数据**：只有在用户主动选择时才生成，并必须明确标记“估算”。

### 2.3 本地优先、隐私优先

MVP 不需要登录，不依赖服务器，不上传交易明细，不申请与核心功能无关的权限。

### 2.4 少操作、多反馈

常用记账应在 3 个主要操作内完成。添加一笔支出后，首页预算、结余、分类图表和账户余额应立即更新。

### 2.5 解释结果，而不只堆砌图表

分析页不仅显示数值，还要用规则化中文摘要解释：

- 本月比上月多花了多少；
- 增长主要来自哪些分类；
- 当前消费速度是否超标；
- 月结余目标完成概率；
- 年目标还差多少；
- 哪些支出可能是异常或一次性支出。

---

## 3. 用户现有数据与导入规则

### 3.1 总体历史数据

| 统计节点 | 总余额 | 周期结余 | 周期收入 | 周期消费 | 精确月均消费 |
|---|---:|---:|---:|---:|---:|
| 2023 年 8 月 | 110,400 | — | — | — | — |
| 2024 年 8 月 | 158,100 | 47,700 | 79,400 | 31,700 | 2,641.67 |
| 2025 年 8 月 | 225,020 | 66,920 | 104,400 | 37,480 | 3,123.33 |
| 2026 年 8 月 | 296,450 | 71,430 | 124,400 | 52,970 | 4,414.17 |

年度周期默认解释为：

- FY2024：2023-08-01 至 2024-07-31；
- FY2025：2024-08-01 至 2025-07-31；
- FY2026：2025-08-01 至 2026-07-31。

此时间范围必须在导入向导中可编辑。App 同时支持：

- 自然年：1 月至 12 月；
- 自定义财务年：默认 9 月至次年 8 月。

### 3.2 各账户余额快照

所有金额单位为元。

| 月份 | 招商银行 | 农商银行 | 农业银行 | 中国银行 | 建设银行 | 微众银行 | 微信 | 支付宝 | 合计 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2023-08 | 40,000 | 3,300 | 10,400 | 800 | 400 | 0 | 43,600 | 11,900 | 110,400 |
| 2024-08 | 51,500 | 7,400 | 100 | 300 | 64,000 | 0 | 22,800 | 12,000 | 158,100 |
| 2025-08 | 102,700 | 0 | -1,600 | 900 | 42,200 | 70,400 | 520 | 9,900 | 225,020 |
| 2026-08 | 103,900 | 400 | -1,800 | 200 | 80,000 | 101,300 | 2,350 | 10,100 | 296,450 |

原始微信和支付宝数据包含加法表达式，应在导入时拆成可重命名子账户：

- 2023-08 微信：3,500 + 40,100；
- 2023-08 支付宝：10,100 + 1,800；
- 2024-08 微信：1,800 + 21,000；
- 2024-08 支付宝：9,800 + 2,200；
- 2025-08 微信：170 + 350；
- 2025-08 支付宝：9,200 + 700；
- 2026-08 微信：2,000 + 350；
- 2026-08 支付宝：9,200 + 900。

由于子账户实际名称未知，首次导入时暂命名为：

- 微信子账户 A；
- 微信子账户 B；
- 支付宝子账户 A；
- 支付宝子账户 B。

用户可以在导入向导内改为“微信零钱”“零钱通”“支付宝余额”“余额宝”等真实名称。

### 3.3 负余额处理

农业银行在 2025、2026 年出现负余额。系统必须允许账户余额为负数，不得自动纠正为零。负余额可以表示透支、待还款或录入状态，账户类型可由用户设置为：

- 资产账户；
- 负债账户；
- 可正可负账户。

### 3.4 历史数据展示规则

1. 资产趋势图可以从 2023-08 起展示真实余额快照。
2. 年度收入、消费、结余图可以展示 FY2024—FY2026 的真实汇总。
3. 月度趋势图只展示真实月度流水。
4. 没有真实月度流水的历史月份显示“无明细”，不可显示为零消费。
5. 用户可以选择“仅用于趋势的均摊估算”，但估算数据必须使用虚线、浅色和“估算”标签，且不参与默认预算评价。

---

## 4. 产品目标与非目标

### 4.1 MVP 必须实现

- 账户及子账户管理；
- 收入、支出、转账、退款、余额调整；
- 月度首页；
- 月消费和年消费目标；
- 月结余和年结余目标；
- 月收入和年收入目标；
- 分类预算；
- 资产余额目标；
- 月度、年度、多维分析；
- 历史余额快照和年度汇总导入；
- CSV 导入、导出；
- 本地数据库；
- 数据备份和恢复；
- 深色模式；
- 记账提醒和月度复盘提醒；
- 单元测试、数据库测试、主要界面测试；
- 可安装 APK 的完整工程。

### 4.2 后续增强功能

- 系统桌面小组件；
- 收据图片 OCR；
- 在获得明确授权后解析通知或短信中的交易信息；
- 重复支出智能识别；
- 商户自动分类；
- 多币种与汇率；
- WebDAV 或用户自有云盘同步；
- 家庭共享账本；
- 投资账户和收益率分析；
- 自然语言问答；
- AI 月报。

### 4.3 MVP 不做

- 不接入银行账号和网银密码；
- 不使用无障碍服务抓取其他 App；
- 不默认读取短信、通知、相册或定位；
- 不将年度数据伪造成逐月交易；
- 不实现股票交易或投资建议；
- 不强制用户注册账号。

---

## 5. 信息架构

底部导航固定为五项：

1. **首页**
2. **流水**
3. **分析**
4. **目标**
5. **我的**

全局右下角使用悬浮按钮“＋”，点击后打开快速记账底部弹窗。

### 5.1 首页

首页以当前月为主，包含：

1. 月份切换器；
2. 本月消费；
3. 本月预算剩余；
4. 本月收入；
5. 本月结余；
6. 本月储蓄率；
7. 月底预测消费；
8. 月底预测结余；
9. 预算进度环；
10. 近 7 天消费趋势；
11. 分类消费前 5；
12. 本月与上月对比；
13. 年度目标进度；
14. 总资产卡片；
15. 账户余额分布；
16. 智能提示；
17. 快速记账入口。

### 5.2 流水

包含：

- 时间轴列表；
- 日历视图；
- 搜索；
- 筛选；
- 批量编辑；
- 交易详情；
- 重复交易模板；
- 导入批次；
- 待确认交易。

筛选维度：

- 月份或自定义日期；
- 收入、支出、转账、退款、调整；
- 分类；
- 账户；
- 商户；
- 标签；
- 必要/可选；
- 固定/可变；
- 是否报销；
- 是否一次性；
- 金额区间；
- 数据来源。

### 5.3 分析

分析页采用顶部时间范围切换和分析模块卡片。默认范围为本月。

一级维度：

- 概览；
- 消费；
- 收入；
- 结余；
- 资产；
- 账户；
- 行为；
- 数据质量。

### 5.4 目标

目标类型：

- 月消费上限；
- 年消费上限；
- 月分类预算；
- 年分类预算；
- 月收入目标；
- 年收入目标；
- 月结余目标；
- 年结余目标；
- 月储蓄率目标；
- 年储蓄率目标；
- 指定日期资产余额目标；
- 应急金覆盖月数目标；
- 自定义金额目标。

### 5.5 我的

包含：

- 账户管理；
- 分类和标签；
- 周期设置；
- 货币和数字格式；
- 主题；
- 提醒；
- 数据导入；
- 数据导出；
- 备份与恢复；
- 隐私说明；
- 关于；
- 开发者诊断信息。

---

## 6. 核心页面详细规格

## 6.1 首次启动与导入向导

### 第一步：欢迎页

文案：

> 从这个月开始，看清每一笔钱去了哪里。  
> 数据默认只保存在你的手机中。

按钮：

- 使用已有历史数据；
- 创建空白账本。

### 第二步：统计周期

用户选择：

- 自然年；
- 9 月至次年 8 月；
- 自定义起始月。

默认选中“9 月至次年 8 月”。

### 第三步：确认账户

预置：

- 招商银行；
- 农商银行；
- 农业银行；
- 中国银行；
- 建设银行；
- 微众银行；
- 微信子账户 A/B；
- 支付宝子账户 A/B。

支持修改名称、图标、账户类型、是否计入总资产。

### 第四步：导入余额快照

展示四个历史月份，每行显示合计和账户明细。合计不一致时禁止继续，并提示差额。

### 第五步：导入年度汇总

展示 FY2024—FY2026 的收入、消费、结余。系统自动校验：

`收入 - 消费 = 结余`

### 第六步：设置首批目标

推荐默认值仅作为建议，不自动保存：

- 月消费目标：5,000 元；
- 年消费目标：60,000 元；
- 月结余目标：6,000 元；
- 年结余目标：72,000 元；
- 年储蓄率目标：60%；
- 资产目标：300,000 元。

用户确认后保存。

### 第七步：进入首页

显示导入成功摘要：

- 4 个月份余额快照；
- 3 个年度汇总；
- 10 个账户或子账户；
- 当前总余额。

---

## 6.2 首页布局

### 顶部区域

- 左侧：当前年月，例如“2026 年 8 月”；
- 点击打开月份选择器；
- 右侧：眼睛图标，用于隐藏金额；
- 右侧：通知图标。

### 核心总览卡

大号数字：本月消费。

下方四项：

- 预算剩余；
- 本月收入；
- 本月结余；
- 储蓄率。

预算状态：

- 0%—69%：正常；
- 70%—89%：接近目标；
- 90%—100%：预警；
- 超过 100%：超支。

不能只依赖颜色，必须同时显示文字和图标。

### 月底预测卡

显示：

- 当前日均消费；
- 预测月底消费；
- 预计超过或低于预算金额；
- 预测月底结余；
- 预测置信说明。

示例：

> 按当前速度，本月预计消费 5,420 元，可能超过目标 420 元。餐饮和旅行是主要增长来源。

### 分类卡

默认显示前 5 个消费分类，使用横向条形图。点击进入分类分析。

### 趋势卡

显示最近 7 天或本月每日消费折线图。支持切换：

- 每日；
- 累计；
- 与上月同期对比。

### 年目标卡

显示：

- 年消费进度；
- 年结余进度；
- 年收入进度；
- 资产目标进度。

### 资产卡

显示：

- 当前总资产；
- 较上一个余额快照变化；
- 资产可覆盖月数；
- 最大账户占比；
- 点击进入账户分析。

### 智能提示卡

最多展示 3 条最重要提示。示例：

- 本月消费速度比预算进度快 18%；
- 旅行支出较近 3 个月均值增加 1,260 元；
- 距离 30 万元资产目标还差 3,550 元；
- 本月已有 6 个无消费日；
- 本月结余目标按当前趋势可以完成。

提示由确定性规则引擎生成，不依赖网络和大语言模型。

---

## 6.3 快速记账

点击全局“＋”后，从底部弹出记账面板。

顶部类型切换：

- 支出；
- 收入；
- 转账；
- 退款；
- 调整。

### 支出必填项

- 金额；
- 分类；
- 支出账户；
- 日期。

### 支出选填项

- 子分类；
- 商户；
- 时间；
- 标签；
- 备注；
- 必要/可选；
- 固定/可变；
- 一次性；
- 可报销；
- 图片附件；
- 是否建立重复规则。

### 交互要求

- 金额键盘直接弹出；
- 默认选择上一次使用账户；
- 根据时间、商户和历史记录推荐分类；
- 保存后显示 2 秒撤销提示；
- 支持长按“＋”直接进入支出；
- 支持复制上一笔；
- 支持创建常用模板，例如“早餐 10 元”“停车 8 元”。

### 转账处理

转账必须同时包含转出账户和转入账户，只改变账户余额，不计入收入、消费和结余。

### 退款处理

退款可关联原支出。默认抵减原分类消费，并保留原始交易关系。

### 余额调整

用于实际余额与账面余额不一致时校准。调整不计入收入和消费，但要记录调整原因，并在数据质量页可见。

---

## 7. 分类体系

系统预置分类可编辑。

### 7.1 支出分类

- 餐饮；
- 交通；
- 住房；
- 水电燃气；
- 通讯网络；
- 日用购物；
- 服饰美妆；
- 数码家电；
- 医疗健康；
- 宠物；
- 旅行；
- 娱乐；
- 学习；
- 人情往来；
- 家庭支出；
- 工作经营；
- 保险；
- 税费；
- 捐赠；
- 其他。

### 7.2 收入分类

- 工资；
- 奖金；
- 兼职；
- 经营收入；
- 报销；
- 利息；
- 投资收益；
- 退款；
- 礼金；
- 其他。

### 7.3 分类属性

每个分类可设置：

- 必要或可选；
- 固定或可变；
- 是否计入常规生活成本；
- 是否计入应急金计算；
- 图标；
- 排序；
- 预算；
- 是否启用。

---

## 8. 目标和预算系统

## 8.1 目标模型

目标至少包含：

- 名称；
- 目标类型；
- 周期；
- 开始日期；
- 结束日期；
- 目标金额或比例；
- 当前进度；
- 是否循环；
- 是否结转；
- 提醒阈值；
- 是否启用；
- 备注。

## 8.2 月消费目标

计算：

`月消费进度 = 本月有效支出 / 月消费目标`

显示：

- 已花；
- 剩余；
- 剩余天数；
- 建议日均可花；
- 当前消费速度；
- 预测月底消费。

## 8.3 年消费目标

支持自然年和财务年。显示：

- 已消费；
- 年目标剩余；
- 时间进度；
- 消费进度；
- 当前是否超前；
- 预计年末消费。

## 8.4 月结余目标

`月结余 = 月收入 - 月支出`

转账和余额调整不参与计算。

如果收入为零，储蓄率显示为“不可计算”，不得显示无穷或 0%。

## 8.5 年结余目标

累计年收入减累计年支出。可与用户历史 FY 数据连续展示。

## 8.6 资产目标

示例：

- 30 万元；
- 40 万元；
- 50 万元；
- 100 万元。

显示：

- 当前总资产；
- 目标差额；
- 当前完成比例；
- 按近 3 个月平均结余测算的预计达成日期；
- 保守、基准、乐观三种情景。

## 8.7 目标结转

月预算支持：

- 不结转；
- 剩余额度结转；
- 超支金额结转；
- 全部净额结转。

默认不结转，避免规则过于复杂。

---

## 9. 分析指标体系

## 9.1 时间维度

- 日；
- 周；
- 月；
- 季度；
- 自然年；
- 自定义财务年；
- 最近 3 个月；
- 最近 6 个月；
- 最近 12 个月；
- 自定义日期范围；
- 同比；
- 环比；
- 与近 3 个月均值对比；
- 与近 12 个月中位数对比。

## 9.2 核心财务指标

1. 收入；
2. 消费；
3. 结余；
4. 储蓄率；
5. 消费率；
6. 月均收入；
7. 月均消费；
8. 月均结余；
9. 总资产；
10. 净资产；
11. 资产增长额；
12. 资产增长率；
13. 可支配现金；
14. 应急金覆盖月数；
15. 固定支出占比；
16. 可选支出占比；
17. 重复支出占比；
18. 一次性支出占比；
19. 最大分类占比；
20. 最大商户占比；
21. 最大账户占比；
22. 无消费天数；
23. 日均消费；
24. 消费波动率；
25. 预算使用率；
26. 时间进度；
27. 消费速度偏差；
28. 目标完成率；
29. 预计月底消费；
30. 预计月底结余；
31. 预计年末消费；
32. 预计年末结余；
33. 资产里程碑预计日期；
34. 数据完整率；
35. 账面与快照差额。

## 9.3 消费分析

图表和卡片：

- 分类环形图；
- 分类横向条形图；
- 月度消费折线；
- 累计消费曲线；
- 本月与上月同期曲线；
- 固定/可变消费；
- 必要/可选消费；
- 一次性/经常性消费；
- 商户排行；
- 标签排行；
- 周几消费分布；
- 每月日期分布；
- 小时段分布；
- 大额支出列表；
- 异常支出列表；
- 无消费日历；
- 月度消费热力图。

### 异常支出规则

满足任一规则可标记为异常候选：

- 单笔金额高于过去 90 天同分类中位数的 3 倍；
- 单笔金额进入过去 12 个月支出前 5%；
- 某分类本月消费高于近 3 个完整月平均值 50%；
- 新出现的高金额商户；
- 重复扣费金额变化超过 20%。

异常只是提示，不自动判定为错误。

## 9.4 收入分析

- 收入来源占比；
- 收入稳定性；
- 固定收入和非固定收入；
- 月收入趋势；
- 同比与环比；
- 最大收入来源；
- 收入目标完成度；
- 收入波动；
- 新增收入有多少转化为新增结余。

### 新增收入转化率

`新增收入转化为结余比例 = 结余同比增量 / 收入同比增量`

当收入同比增量为零或负数时不计算，并给出解释。

## 9.5 结余分析

- 月结余趋势；
- 年结余趋势；
- 储蓄率趋势；
- 连续正结余月数；
- 结余最佳月份；
- 结余最差月份；
- 目标差距；
- 收入增长和消费增长对结余的贡献分解。

## 9.6 资产分析

- 总资产趋势；
- 账户余额堆叠图；
- 账户占比；
- 现金类资产；
- 负债；
- 净资产；
- 资产集中度；
- 余额快照变化；
- 资产目标；
- 可覆盖生活月数；
- 历史里程碑。

### 资产集中度

显示最大账户占比，不需要使用复杂金融术语。示例：

> 当前 35% 的已统计资金集中在微众银行。

## 9.7 行为分析

- 最常消费的星期；
- 最常消费的时间段；
- 月初、月中、月末支出比例；
- 无消费日；
- 连续记账天数；
- 记账完整度；
- 冲动消费候选；
- 夜间消费；
- 周末和工作日消费差异。

## 9.8 数据质量分析

- 是否存在未分类交易；
- 是否存在重复交易；
- 是否存在余额不一致；
- 是否存在只有年度汇总、无月度明细；
- 是否存在失效账户；
- 是否存在未完成转账配对；
- 是否存在异常负余额；
- 最近备份时间；
- 导入批次错误。

---

## 10. 关键计算口径

所有金额使用最小货币单位整数存储。人民币金额使用“分”，Kotlin 类型使用 `Long`，禁止使用 `Double` 存储金额。

### 10.1 有效支出

`有效支出 = 支出 - 已确认退款`

不包含：

- 转账；
- 余额调整；
- 被删除交易；
- 草稿；
- 被用户标记为“不计入预算”的交易。

### 10.2 结余

`结余 = 有效收入 - 有效支出`

### 10.3 储蓄率

`储蓄率 = 结余 / 有效收入`

收入小于等于零时返回空值，并显示“本期无有效收入”。

### 10.4 总资产

`总资产 = 所有启用且计入资产的账户余额之和`

### 10.5 净资产

`净资产 = 资产账户余额合计 - 负债账户欠款绝对值`

账户应明确资产/负债属性，不能仅根据正负号猜测。

### 10.6 应急金覆盖月数

`覆盖月数 = 可动用资产 / 近 3 个完整月的平均必要支出`

若真实月度数据不足 3 个月，显示“数据不足”，允许用户选择用年度平均消费作为估算，但必须标记为估算。

### 10.7 预算时间进度

`时间进度 = 已经过天数 / 当月总天数`

### 10.8 消费速度偏差

`消费速度偏差 = 预算使用率 - 时间进度`

例如时间过了 50%，预算已花 70%，偏差为 +20 个百分点。

### 10.9 月底消费预测

建议使用分层规则：

1. 当月真实数据少于 5 天：以近 3 个完整月同期消费和固定计划支出为主；
2. 当月真实数据达到 5 天：  
   `预测变量支出 = 当前变量支出 / 已经过天数 × 当月总天数`；
3. 加上尚未发生的固定重复支出；
4. 对明显一次性大额支出不做线性外推；
5. 显示预测依据和“仅供参考”。

### 10.10 资产目标预计日期

使用近 3 个完整月平均结余作为基准。计算三种情景：

- 保守：平均结余 × 80%；
- 基准：平均结余；
- 乐观：平均结余 × 120%。

若平均结余小于等于零，不输出达成日期。

---

## 11. 规则化财务洞察

建立 `InsightEngine`，输入聚合指标，输出最多 3 条优先级最高的提示。

### 11.1 提示类型

- 预算预警；
- 月底预测超支；
- 月结余目标风险；
- 年目标风险；
- 分类异常增长；
- 单笔异常；
- 固定支出上涨；
- 收入下降；
- 储蓄率下降；
- 资产里程碑；
- 账户余额异常；
- 数据缺失；
- 长时间未备份。

### 11.2 优先级

1. 数据错误或余额不一致；
2. 预算已经超支；
3. 预计无法完成目标；
4. 异常支出；
5. 趋势变化；
6. 正向里程碑；
7. 普通习惯提示。

### 11.3 示例规则

- 月预算使用率 ≥ 90% 且时间进度 < 80%：高优先级；
- 预测消费 > 月目标：高优先级；
- 当前月储蓄率比近 3 月平均低 10 个百分点：中优先级；
- 某分类同比或环比增长超过 50% 且金额增量超过 300 元：中优先级；
- 资产距离整数目标不足一个月平均结余：正向提示；
- 30 天未备份：数据安全提示。

---

## 12. 视觉与交互设计

## 12.1 视觉方向

风格关键词：

- 简洁；
- 温和；
- 高级；
- 清晰；
- 非银行式压迫感；
- 非游戏化；
- 信息密度适中。

建议设计语言：

- Material 3；
- 大圆角卡片；
- 轻微层次阴影；
- 充足留白；
- 金额使用清晰的大号等宽数字或 tabular figures；
- 图表动画 250—400ms；
- 不使用过度渐变；
- 正负变化同时使用图标、文字和颜色。

### 建议配色

浅色模式：

- 主色：深青蓝；
- 背景：微暖白；
- 卡片：纯白；
- 正向：深绿色；
- 预警：琥珀色；
- 超支：克制的红色；
- 次要文字：中性灰。

深色模式：

- 深灰蓝背景；
- 卡片使用不同明度，不使用纯黑；
- 图表保持足够对比度。

支持 Android 动态取色，但需要提供固定品牌色作为关闭动态取色时的方案。

## 12.2 图表规范

- 折线图支持长按查看数值；
- 柱状图支持点击分类；
- 饼图最多直接显示 6 类，其余合并为“其他”；
- 所有图表必须有文字摘要；
- 图表加载为空时显示原因；
- 估算数据使用虚线或斜纹；
- 负数要显示零轴；
- 金额可切换精确值和简写，如 29.65 万。

## 12.3 无障碍

- 可点击区域至少 48dp；
- 支持系统字体放大；
- 颜色不是唯一信息载体；
- 图表提供语义描述；
- 金额隐藏功能应覆盖所有关键页面；
- 深色模式遵循系统或手动设置；
- 支持 TalkBack 基本导航。

## 12.4 自适应布局

手机使用底部导航。平板和折叠屏使用导航栏或列表—详情双栏布局。应用内容不能固定为单一手机宽度。

---

## 13. 技术方案

## 13.1 技术栈

- Kotlin；
- Jetpack Compose；
- Material 3；
- 单 Activity；
- Navigation Compose；
- ViewModel；
- Kotlin Coroutines；
- StateFlow；
- Room；
- DataStore；
- WorkManager；
- Hilt；
- Kotlin Serialization；
- JUnit；
- AndroidX Test；
- Compose UI Test。

版本策略：

- 使用实现时的 AndroidX 最新稳定版；
- 不使用 alpha、beta、RC 依赖，除非某功能无稳定替代且在代码注释中说明；
- 使用 Gradle Version Catalog 集中管理版本；
- 固定依赖版本并提交 lockfile 或版本目录；
- `minSdk = 26`；
- `compileSdk` 和 `targetSdk` 使用实现时可用的最新稳定 API；
- Java/Kotlin 工具链使用当前 Android Gradle Plugin 官方支持版本。

图表实现：

- 折线图和柱状图可使用稳定的 Compose 图表库；
- 环形图、预算进度环和热力图优先使用 Compose Canvas 自定义；
- 将图表封装在独立 `core:designsystem` 或 `core:charts` 模块，避免业务页面依赖具体图表库 API。

## 13.2 架构

采用分层架构和单向数据流：

- UI Layer；
- Domain Layer；
- Data Layer。

推荐模块：

```text
app
core:model
core:database
core:datastore
core:designsystem
core:charts
core:common
core:testing
feature:onboarding
feature:dashboard
feature:transactions
feature:analytics
feature:goals
feature:accounts
feature:settings
feature:importexport
```

若 Codex 首次生成多模块工程复杂度过高，可先使用单 app 模块，但包结构必须保持上述边界，后续可平滑拆分。

## 13.3 包结构

```text
com.yueji.finance
├── app
├── core
│   ├── common
│   ├── database
│   ├── datastore
│   ├── designsystem
│   ├── charts
│   └── model
├── data
│   ├── account
│   ├── transaction
│   ├── budget
│   ├── goal
│   ├── analytics
│   └── importexport
├── domain
│   ├── calculation
│   ├── insight
│   ├── forecast
│   └── usecase
└── feature
    ├── onboarding
    ├── dashboard
    ├── transactions
    ├── analytics
    ├── goals
    ├── accounts
    └── settings
```

## 13.4 状态管理

每个页面包含：

- immutable `UiState`；
- sealed `UiAction`；
- 一次性 `UiEffect`；
- ViewModel；
- Repository；
- UseCase。

UI 不直接访问 DAO。ViewModel 不持有 Activity 或 View 引用。

---

## 14. 数据库设计

## 14.1 AccountEntity

```kotlin
data class AccountEntity(
    val id: String,
    val name: String,
    val institutionName: String?,
    val parentAccountId: String?,
    val accountType: AccountType,
    val currencyCode: String,
    val openingBalanceMinor: Long,
    val includeInAssets: Boolean,
    val allowNegativeBalance: Boolean,
    val isArchived: Boolean,
    val iconKey: String,
    val colorArgb: Long?,
    val sortOrder: Int,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

`AccountType`：

- CASH；
- BANK;
- PAYMENT_PLATFORM；
- SAVINGS；
- INVESTMENT；
- CREDIT；
- LIABILITY；
- OTHER。

## 14.2 TransactionEntity

```kotlin
data class TransactionEntity(
    val id: String,
    val type: TransactionType,
    val amountMinor: Long,
    val currencyCode: String,
    val occurredAt: Instant,
    val localDate: LocalDate,
    val accountId: String,
    val destinationAccountId: String?,
    val categoryId: String?,
    val subcategoryId: String?,
    val merchantId: String?,
    val note: String?,
    val necessity: Necessity?,
    val variability: Variability?,
    val isOneOff: Boolean,
    val isReimbursable: Boolean,
    val excludeFromBudget: Boolean,
    val linkedTransactionId: String?,
    val recurringRuleId: String?,
    val importBatchId: String?,
    val source: TransactionSource,
    val status: TransactionStatus,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

`TransactionType`：

- EXPENSE；
- INCOME；
- TRANSFER；
- REFUND；
- BALANCE_ADJUSTMENT。

金额始终存正数，方向由类型和账户决定。

## 14.3 CategoryEntity

字段：

- id；
- parentId；
- name；
- transactionDirection；
- iconKey；
- defaultNecessity；
- defaultVariability；
- includeInLivingCost；
- isSystem；
- isArchived；
- sortOrder。

## 14.4 TagEntity 与 TransactionTagCrossRef

支持一笔交易多个标签。

## 14.5 MerchantEntity

字段：

- id；
- normalizedName；
- displayName；
- defaultCategoryId；
- lastUsedAt；
- useCount。

## 14.6 BudgetEntity

字段：

- id；
- name；
- periodType；
- startDate；
- endDate；
- targetAmountMinor；
- categoryId；
- accountId；
- rolloverMode；
- warningThresholdPercent；
- isRecurring；
- isEnabled。

## 14.7 GoalEntity

字段：

- id；
- goalType；
- name；
- targetAmountMinor；
- targetRatioBasisPoints；
- startDate；
- targetDate；
- periodType；
- fiscalYearStartMonth；
- isRecurring；
- status；
- reminderEnabled；
- createdAt；
- updatedAt。

比例使用 basis points。例如 60.00% 存为 6000。

## 14.8 BalanceSnapshotEntity

字段：

- id；
- accountId；
- snapshotYearMonth；
- amountMinor；
- datePrecision；
- source；
- note；
- createdAt。

`datePrecision`：

- EXACT_DATE；
- MONTH_ONLY。

用户现有快照使用 `MONTH_ONLY`。

## 14.9 LegacyAnnualSummaryEntity

字段：

- id；
- label；
- periodStartYearMonth；
- periodEndYearMonth；
- incomeMinor；
- expenseMinor；
- savingsMinor；
- isEstimated；
- note。

历史年度汇总与真实逐笔流水分开存储。

## 14.10 RecurringRuleEntity

字段：

- id；
- templateTransactionId；
- frequency；
- interval；
- dayOfMonth；
- dayOfWeek；
- startDate；
- endDate；
- nextOccurrence；
- autoCreate；
- reminderOnly；
- isEnabled。

默认仅提醒或生成“待确认交易”，不默认自动入账。

## 14.11 ImportBatchEntity

记录：

- 文件名；
- 文件哈希；
- 导入时间；
- 成功数量；
- 跳过数量；
- 错误数量；
- 来源；
- 错误报告路径。

用于避免重复导入。

## 14.12 AttachmentEntity

附件保存在应用私有目录，数据库只存：

- id；
- transactionId；
- localRelativePath；
- mimeType；
- fileSize；
- createdAt。

## 14.13 索引

至少创建：

- TransactionEntity(localDate)；
- TransactionEntity(accountId, localDate)；
- TransactionEntity(categoryId, localDate)；
- TransactionEntity(merchantId, localDate)；
- TransactionEntity(type, localDate)；
- BalanceSnapshotEntity(accountId, snapshotYearMonth) 唯一索引；
- LegacyAnnualSummaryEntity(periodStartYearMonth, periodEndYearMonth) 唯一索引；
- ImportBatchEntity(fileHash) 唯一索引。

---

## 15. Repository 和 UseCase

### Repository

- AccountRepository；
- TransactionRepository；
- CategoryRepository；
- BudgetRepository；
- GoalRepository；
- AnalyticsRepository；
- SnapshotRepository；
- LegacySummaryRepository；
- ImportExportRepository；
- SettingsRepository；
- BackupRepository。

### 核心 UseCase

- AddTransactionUseCase；
- UpdateTransactionUseCase；
- DeleteTransactionUseCase；
- AddTransferUseCase；
- AddRefundUseCase；
- ReconcileAccountBalanceUseCase；
- GetMonthlyDashboardUseCase；
- GetMonthlyExpenseAnalysisUseCase；
- GetAnnualAnalysisUseCase；
- CalculateSavingsRateUseCase；
- CalculateRunwayUseCase；
- ForecastMonthEndUseCase；
- ForecastGoalDateUseCase；
- GenerateInsightsUseCase；
- ImportLegacyDataUseCase；
- ImportCsvUseCase；
- ExportCsvUseCase；
- CreateBackupUseCase；
- RestoreBackupUseCase。

---

## 16. DAO 和查询要求

尽量由 Room SQL 完成聚合，避免读取全部交易后在 UI 层计算。

需要提供：

- 指定日期范围收入合计；
- 指定日期范围支出合计；
- 分类汇总；
- 账户汇总；
- 商户汇总；
- 每日汇总；
- 每月汇总；
- 必要/可选汇总；
- 固定/可变汇总；
- 一次性/重复汇总；
- 无分类数量；
- 最大支出；
- 同比和环比所需数据；
- 账户账面余额；
- 快照差额；
- 月目标和年目标进度。

所有查询返回 `Flow`，页面数据变化后自动刷新。

---

## 17. CSV 导入导出

## 17.1 标准交易 CSV

UTF-8，首行字段：

```csv
id,type,amount,currency,date,time,account,destination_account,category,subcategory,merchant,tags,note,necessity,variability,is_one_off,is_reimbursable,exclude_from_budget
```

示例：

```csv
,EXPENSE,28.50,CNY,2026-08-04,12:30,微信子账户A,,餐饮,午餐,某餐厅,工作日,午餐,NECESSARY,VARIABLE,false,false,false
```

## 17.2 导入流程

1. 选择文件；
2. 自动识别编码和分隔符；
3. 字段映射；
4. 预览前 20 行；
5. 校验金额、日期、账户和分类；
6. 显示将新增、跳过和错误数量；
7. 用户确认；
8. 在数据库事务内导入；
9. 输出错误报告。

### 去重

优先使用原始 ID。没有 ID 时计算指纹：

`日期 + 时间 + 金额 + 类型 + 账户 + 商户 + 备注`

重复项默认跳过，允许用户覆盖。

## 17.3 导出

支持：

- 全部数据；
- 指定日期范围；
- 指定账户；
- 指定分类；
- 月报；
- 年报；
- 账户快照；
- 完整备份。

---

## 18. 备份与恢复

### 18.1 本地备份格式

生成单个 `.yueji` 文件，本质为 ZIP，包含：

```text
manifest.json
database.sqlite
preferences.json
attachments/
checksum.json
```

### 18.2 安全

- 用户主动选择导出位置；
- 支持可选密码加密；
- 恢复前展示备份日期、版本、交易数和账户数；
- 恢复前自动创建当前数据安全副本；
- 恢复失败必须回滚；
- 不把敏感数据写入日志。

### 18.3 Android 系统备份

允许系统备份必要的数据库和设置，但附件和临时文件应按规则选择性包含。无论系统备份是否可用，都必须保留用户手动导出完整备份的能力。

---

## 19. 提醒与后台任务

使用 WorkManager：

- 每日记账提醒；
- 月预算预警；
- 月中检查；
- 月末复盘；
- 年目标检查；
- 定期本地备份提醒；
- 重复交易待确认提醒。

提醒必须可关闭，并支持设置时间。默认不开启高频提醒。

建议默认：

- 每月 15 日：月中预算检查；
- 每月最后一天：月度复盘；
- 30 天未备份：备份提醒。

---

## 20. 安全与隐私

1. MVP 不申请网络权限，除非依赖库或后续云功能明确需要。
2. 数据库存储在应用私有目录。
3. 附件存储在应用私有目录。
4. 提供应用锁，可使用设备凭据或生物识别。
5. 最近任务缩略图可选择隐藏金额。
6. 日志中不得记录金额、账户余额、备注和商户。
7. 崩溃报告默认关闭或仅使用不含财务内容的匿名技术信息。
8. 清空数据需要二次确认并输入确认词。
9. 导出文件提醒用户妥善保管。
10. 自动解析短信或通知只能作为后续功能，必须显式授权并允许逐条确认。

---

## 21. 性能要求

在中端 Android 设备上，以 10,000 条交易数据作为测试基准：

- 冷启动后首页可交互时间目标小于 2 秒；
- 已加载数据库时切换月份目标小于 500ms；
- 新增交易后关键指标在 300ms 内更新；
- 流水列表滚动无明显卡顿；
- 图表不阻塞主线程；
- CSV 导入使用后台线程并展示进度；
- 数据库迁移和恢复可中断保护；
- 不在 Compose 重组中执行昂贵聚合。

这些是工程目标，应通过基准测试和 Profile 验证，而不是硬编码假设。

---

## 22. 测试要求

## 22.1 单元测试

必须覆盖：

- 收入、支出、结余；
- 储蓄率；
- 转账不计入收支；
- 退款抵减支出；
- 余额调整；
- 预算进度；
- 年度周期；
- 9 月至次年 8 月财务年；
- 月底预测；
- 资产目标日期；
- 应急金覆盖月数；
- 负余额；
- 金额精度；
- 无收入时储蓄率；
- 跨年、闰年、月末；
- 规则化提示优先级。

## 22.2 数据库测试

- DAO 聚合；
- 事务；
- 转账原子性；
- 唯一索引；
- 重复导入；
- 数据库升级迁移；
- 备份恢复；
- 快照校验。

## 22.3 UI 测试

- 首次导入；
- 新增支出；
- 新增收入；
- 转账；
- 修改和删除；
- 月份切换；
- 设置目标；
- 超支状态；
- 金额隐藏；
- 深色模式；
- 大字体；
- 空状态；
- 错误状态；
- 恢复备份。

## 22.4 历史数据验收测试

导入后必须得到：

- 2023-08 总余额：110,400；
- 2024-08 总余额：158,100；
- 2025-08 总余额：225,020；
- 2026-08 总余额：296,450；
- FY2024：79,400 - 31,700 = 47,700；
- FY2025：104,400 - 37,480 = 66,920；
- FY2026：124,400 - 52,970 = 71,430；
- 四个快照的账户合计与总余额完全一致；
- 未生成虚假月度交易；
- 月度分析对历史无明细月份显示“无明细”。

---

## 23. 空状态和错误状态

### 无本月流水

> 本月还没有记录。添加第一笔支出后，这里会显示预算和趋势。

### 只有年度汇总

> 这一时期只有年度汇总，没有逐月明细，因此无法分析具体月份。

### 无收入

> 本期没有有效收入，暂时无法计算储蓄率。

### 余额不一致

> 招商银行账面余额与最新快照相差 320 元。请补充遗漏流水或进行余额调整。

### 导入失败

必须显示具体行号和原因，不得只显示“导入失败”。

---

## 24. 月报和年报

## 24.1 月报

自动生成可阅读卡片：

- 本月收入；
- 本月消费；
- 本月结余；
- 储蓄率；
- 预算结果；
- 消费前三分类；
- 最大单笔支出；
- 与上月对比；
- 无消费日；
- 目标完成情况；
- 下月建议关注项。

支持导出为图片或 PDF，导出时默认隐藏账户详细余额，可由用户打开。

## 24.2 年报

包含：

- 年收入；
- 年消费；
- 年结余；
- 储蓄率；
- 月度趋势；
- 分类分布；
- 资产增长；
- 最佳结余月；
- 最大支出月；
- 年目标；
- 历史年度对比；
- 资产里程碑。

年度对比应同时支持自然年和自定义财务年。

---

## 25. 实施阶段

## 阶段 1：工程基础

- 创建 Android 工程；
- 配置 Compose、Material 3、Hilt、Room、DataStore、Navigation；
- 建立主题、设计系统和导航；
- 建立 CI；
- 建立测试框架。

## 阶段 2：数据层

- 实体；
- DAO；
- Database；
- Repository；
- 金额类型；
- 日期周期；
- 历史数据种子；
- 数据校验。

## 阶段 3：核心记账

- 账户管理；
- 分类管理；
- 新增、编辑、删除交易；
- 转账；
- 退款；
- 余额调整；
- 流水列表和筛选。

## 阶段 4：首页和目标

- 月度仪表盘；
- 预算；
- 月目标；
- 年目标；
- 资产目标；
- 实时刷新。

## 阶段 5：分析

- 聚合查询；
- 图表；
- 环比同比；
- 多维筛选；
- 预测；
- InsightEngine；
- 数据质量。

## 阶段 6：导入导出和备份

- CSV；
- 历史数据导入向导；
- 完整备份；
- 恢复；
- 错误报告。

## 阶段 7：体验优化

- 动画；
- 深色模式；
- 大字体；
- 平板；
- 无障碍；
- 性能；
- 空状态；
- 月报和年报。

## 阶段 8：交付

- 全部测试；
- README；
- 架构说明；
- 数据字典；
- APK；
- 版本更新说明；
- 已知限制。

---

## 26. MVP 验收标准

只有同时满足以下条件，才视为完成：

1. 工程可以在 Android Studio 中直接编译。
2. 可以安装并离线使用。
3. 首次启动可以选择导入本文历史数据。
4. 所有历史合计校验正确。
5. 可以创建、编辑和删除收入与支出。
6. 转账不会增加消费或收入。
7. 首页能按月显示消费、收入、结余和预算。
8. 能设置月消费、年消费、月结余、年结余和资产目标。
9. 分析页至少支持时间、分类、账户、商户和标签维度。
10. 支持自然年和 9 月至次年 8 月财务年。
11. 没有历史月度明细时不会伪造数据。
12. 支持 CSV 导入导出。
13. 支持完整备份和恢复。
14. 支持深色模式和金额隐藏。
15. 主要计算有单元测试。
16. 数据库迁移有测试。
17. 10,000 条交易下主要页面可正常使用。
18. 不依赖服务器即可完成全部 MVP 功能。
19. README 说明如何运行、测试、打包和修改种子数据。
20. 不以静态假数据代替真实数据库功能。

---

## 27. Codex 开发约束

Codex 必须遵守：

1. 先生成完整可编译的工程骨架，再逐功能实现。
2. 每完成一个阶段运行构建和测试。
3. 不创建只有界面的假 App。
4. 不把所有业务逻辑写在 Composable 中。
5. 不使用 `Double` 存金额。
6. 不用字符串直接比较日期。
7. 不把转账当收入和支出。
8. 不为年度历史汇总生成虚假月交易。
9. 不在主线程执行数据库、导入、备份和复杂聚合。
10. 不使用 alpha 依赖。
11. 所有数据库变更提供 migration。
12. 所有关键口径写入代码注释和 README。
13. UI 文案集中管理，禁止大量硬编码散落。
14. 关键功能必须有测试。
15. 发现需求冲突时优先保证数据正确、可解释和可恢复。
16. 每个阶段提交变更摘要、测试结果和待办项。
17. 生成的代码应可维护，而不是只追求一次性运行。
18. 默认不加入联网权限。
19. 使用模拟数据只限 Preview 和测试。
20. 发布构建不得包含真实用户财务数据。

---

## 28. 可直接复制给 Codex 的启动指令

```text
请根据仓库中的《月迹安卓个人财务监控 App：产品需求、交互设计与 Codex 实现规格》实现一个完整的 Android 原生应用。

执行要求：

1. 使用 Kotlin、Jetpack Compose、Material 3、Room、DataStore、WorkManager、Hilt、Coroutines、StateFlow 和 Navigation Compose。
2. 使用单向数据流和分层架构。
3. 金额使用 Long 存储最小货币单位，严禁使用 Double。
4. App 必须本地优先、离线可用，MVP 默认不申请网络权限。
5. 实现账户、子账户、收入、支出、转账、退款、余额调整、预算、目标、分析、CSV、备份恢复。
6. 实现自然年和“9 月至次年 8 月”财务年。
7. 导入文档中的 2023—2026 历史余额快照和 FY2024—FY2026 年度汇总。
8. 不得把年度汇总均摊为虚假的月度流水。
9. 首页必须以当前月为中心，显示本月消费、收入、结余、储蓄率、预算剩余、月底预测、年度目标和总资产。
10. 分析页必须支持时间、分类、账户、商户、标签、必要/可选、固定/可变、一次性/重复等维度。
11. 先创建可编译工程，再按文档实施阶段逐步开发。
12. 每完成一个阶段执行构建、单元测试和相关 UI/数据库测试。
13. 不允许只生成静态页面或伪数据演示。
14. 所有历史合计、转账、退款、目标、财务年和预测口径必须有测试。
15. 提供 README、架构说明、数据字典、运行方法、测试方法和打包方法。
16. 最终输出可安装 APK，并列出完成项、未完成项、测试结果和已知限制。

开始时请先：
A. 检查当前目录和已有文件；
B. 输出实施清单；
C. 创建工程和版本目录；
D. 完成数据模型、数据库与历史种子数据；
E. 运行第一次构建和测试；
F. 再继续实现 UI 和功能。
```

---

## 29. 官方技术依据

实现时优先参考 Android 官方当前稳定文档，尤其是：

- Android App Architecture；
- Jetpack Compose；
- Material 3；
- Material 3 Adaptive；
- Room；
- DataStore；
- WorkManager；
- Android Backup and Restore；
- Android Accessibility；
- Android Testing。

不要仅根据本文写死旧版本号。应选择实现当日兼容的稳定版本，并在 Version Catalog 中统一锁定。

---

## 30. 后续可迭代方向

在 MVP 数据准确、备份可靠和记账体验稳定后，再按以下顺序扩展：

1. 桌面小组件；
2. 重复支出和订阅识别；
3. 收据 OCR；
4. 用户授权的通知交易识别；
5. 自动分类；
6. 加密云同步；
7. 多设备；
8. 投资账户；
9. AI 月报；
10. 家庭共享账本。

优先级原则始终是：

**数据正确 > 数据可恢复 > 记账便捷 > 分析有用 > 界面炫酷。**
